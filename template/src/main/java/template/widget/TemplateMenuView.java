package template.widget;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.ListView;

public class TemplateMenuView extends ListView {

    public TemplateMenuView(Context context) {
        super(context);
    }

    public TemplateMenuView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public TemplateMenuView(Context context, AttributeSet attrs, int defStyleAttr) {
        super(context, attrs, defStyleAttr);
    }
}
